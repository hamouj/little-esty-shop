<h1><%= @bulk_discount.name %></h1>
<h3><%= @bulk_discount.percent_discount %>% off <%= @bulk_discount.quantity_threshold %> items</h3>

<%= link_to "Edit Discount", edit_merchant_bulk_discount_path(@merchant, @bulk_discount) %>
